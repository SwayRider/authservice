meta {
  name: Delete Service Client
  type: http
  seq: 3
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/delete-service-client
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "clientId": "{{service_client_id}}"
  }
}
