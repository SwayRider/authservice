meta {
  name: Refresh
  type: http
  seq: 4
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/refresh
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
