meta {
  name: Check Password Strength
  type: http
  seq: 2
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/check-password-strength
  body: json
  auth: inherit
}

body:json {
  {
    "password": "{{AUTHSERVICE_ADMIN_PASSWORD}}"
  }
}
