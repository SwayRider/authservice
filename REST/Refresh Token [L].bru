meta {
  name: Refresh Token [L]
  type: http
  seq: 8
}

post {
  url: {{AUTHSERVICE_HOST}}:{{AUTHSERVICE_PORT}}/api/v1/auth/refresh
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "refreshToken": "{{refresh_token}}"
  }
}
